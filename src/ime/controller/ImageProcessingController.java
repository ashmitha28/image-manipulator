package ime.controller;

/**
 * This interface acts as a controller in the Model-View-Controller (MVC) architectural pattern,
 * specifically for image processing tasks.
 */
public interface ImageProcessingController {

  /**
   * By invoking this method, the controller will begin prompting user for inputs and executing the
   * various commands provided by the user.
   */
  void execute();

}
